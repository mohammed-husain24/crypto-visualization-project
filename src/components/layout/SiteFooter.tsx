export function SiteFooter() {
  return (
    <footer className="site-footer">
      <div className="site-shell site-footer__inner">
        <p>Cipher Atlas is an educational visualization project, not a production encryption tool.</p>
        <p>
          Use the step views to understand algorithm structure, then rely on audited libraries and platform APIs for real security work.
        </p>
      </div>
    </footer>
  );
}
